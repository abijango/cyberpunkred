//! LLM provider abstraction. The `LlmProvider` trait is consumed by `cpr_web`
//! (browser-direct LM Studio) and `cpr_server` (cloud providers). Prompt-input
//! types are defined here; the data they describe comes from `cpr_rules` /
//! `cpr_gm`.
//!
//! See `IMPLEMENTATION_PLAN.md` §1.2 and Phase 7.

/// WP-000 placeholder. Replaced once WP-700 lands the `LlmProvider` trait.
pub fn placeholder() {}
