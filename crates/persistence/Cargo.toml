[package]
name = "cpr_persistence"
description = "Save/load schemas. Native (sqlx) + WASM (serde-only)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"
