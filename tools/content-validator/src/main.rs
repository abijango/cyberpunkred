//! `content-validator`: load every RON file under `content/` and report
//! schema errors. Real implementation lands in WP-1004.

fn main() {
    println!("content-validator: WP-000 placeholder");
}
